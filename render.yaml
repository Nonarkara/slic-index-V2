services:
  - type: web
    name: slic-index-v2
    runtime: node
    buildCommand: npm install --include=dev && npm run build:render
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
